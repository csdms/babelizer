
Added new subcommands to *babelize* that generate single files rather
than an entire project. Subcommands are *sample-config*, *sample-gitignore*,
*sample-license*, *sample-meson-build*, and *sample-readme*.
